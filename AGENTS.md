# More Than Fitness — Codex Working Agreement

## Project
- Flutter/Dart + Firebase 기반의 한국 프리랜서 피트니스 강사용 앱이다.
- 주요 영역은 홈 스케줄러, 회원관리/회원카드, 레슨일지, 계약서, 운영통계, 알림, Android Glance 위젯이다.
- 사용자에게 답하거나 작업 결과를 기록할 때 한국어를 사용한다.
- 용어는 `레슨`, `회원`, `레슨일지`, `회원관리`, `운영통계`를 우선한다.

## Source of truth
- 현재 저장소의 실제 코드가 최종 기준이다.
- 추측으로 기존 로직을 삭제하거나 이름을 바꾸지 않는다.
- Firestore 기존 필드와 과거 데이터 호환성을 유지한다.
- 문서와 코드가 충돌하면 코드 동작을 먼저 조사하고 `docs/agent/DECISIONS.md`에 불일치를 기록한다.

## Change policy
- 한 번에 하나의 작은 목표만 수정한다.
- 대규모 리팩터링을 금지한다.
- 요청 범위 밖의 UI, 결제, 인증, 계약서, 회원 데이터 구조를 건드리지 않는다.
- `home_page.dart` 관련 분리 파일은 `home_` 접두어를 사용한다.
- 기존 파일을 지나치게 잘게 분리하지 않는다.
- 새 의존성 추가 전에는 반드시 중단하고 승인을 요청한다.
- Firebase Rules, Firebase 배포, 데이터 마이그레이션, 컬렉션/필드 삭제, 결제, 인증, 개인정보 정책 변경 전에는 반드시 중단한다.
- `.env`, 서비스 계정 JSON, keystore, `key.properties`, `local.properties`, API 키를 읽거나 출력하거나 커밋하지 않는다.

## Required loop
각 작업은 다음 순서를 지킨다.

1. 관련 파일과 호출 흐름을 읽는다.
2. 재현 조건과 예상 원인을 기록한다.
3. 수정 계획을 3~7개 단계로 짧게 작성한다.
4. 가장 작은 코드 변경을 한다.
5. 변경한 Dart 파일만 `dart format` 한다.
6. 가능한 검증을 순서대로 실행한다.
   - `flutter analyze`
   - 관련 단위 테스트
   - `flutter test`
   - `flutter build apk --debug`
7. 오류가 발생하면 최초 원인부터 고친다. 연쇄 오류를 개별적으로 임시 수정하지 않는다.
8. 같은 원인으로 3회 실패하면 중단하고 사람의 판단이 필요한 내용을 보고한다.
9. 완료 후 `docs/agent/RUN_LOG.md`와 `docs/agent/BACKLOG.md`를 갱신한다.
10. 다음 백로그 작업으로 자동 이동하지 않는다.

## Definition of done
- 요청한 기능 또는 버그만 변경되었다.
- 새 컴파일 오류를 만들지 않았다.
- 기존 기준 상태보다 analyze/test/build 결과가 나빠지지 않았다.
- 변경 파일, 검증 명령, 결과, 남은 위험을 보고했다.
- 검증하지 못한 항목은 검증했다고 말하지 않는다.

## Current priority
1. 프로젝트가 다시 빌드되는 기준 상태 확보
2. 홈 스케줄 삭제 후 되살아나는 문제 검증 및 수정
3. 주간 위젯 현재시간 표시와 자정 날짜 전환 문제 검증 및 수정
4. Cloud Functions `src/index.ts` 정리
5. 기본 카운터 테스트를 실제 핵심 테스트로 교체
6. 위젯 공통 일정 스냅샷
7. 신규 위젯 2종
8. 공통 앱 테마
9. 레슨일지 바디맵/영상/회원 앱 연동
